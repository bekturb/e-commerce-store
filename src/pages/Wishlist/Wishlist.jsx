import React from 'react';
import Helmet from "../../layout/Helmet";

const Wishlist = () => {
    return (
        <Helmet title="Wishlist">
            <div>
                wishlist
            </div>
        </Helmet>
    );
};

export default Wishlist;